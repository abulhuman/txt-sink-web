import {
    ThemeProvider,
    Card, CardBody, CardHeader, CardFooter,
    Dialog, /*DialogHeader, DialogBody, DialogFooter,*/
    Input, Checkbox, Select, /*Option,*/ Button, IconButton,
    Typography,
    Collapse, Navbar, Chip, Tooltip,
    Tabs,
    // Tab, TabsHeader, TabsBody, TabPanel,
    List, ListItem,
    // ListItemPrefix, ListItemSuffix,
    Menu,
    // MenuHandler, MenuList,
    MenuItem,
    Popover,
    // PopoverHandler,
    PopoverContent,
    Badge, Spinner, 
} from "@material-tailwind/react";

export {
    ThemeProvider,
    Card, CardBody, CardHeader, CardFooter,
    Dialog, /*DialogHeader, DialogBody, DialogFooter,*/
    Input, Checkbox, Select, /*Option,*/ Button, IconButton,
    Typography,
    Collapse, Navbar, Chip, Tooltip,
    Tabs,
    // Tab, TabsHeader, TabsBody, TabPanel,
    List, ListItem,
    // ListItemPrefix, ListItemSuffix,
    Menu,
    // MenuHandler, MenuList,
    MenuItem,
    Popover,
    // PopoverHandler,
    PopoverContent,
    Badge, Spinner,
}; 